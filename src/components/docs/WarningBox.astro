---
---
<aside class="callout callout--warning not-content">
  <p class="callout__label">
    <i class="ti ti-alert-triangle" aria-hidden="true"></i>
    Warning
  </p>
  <div class="callout__body"><slot /></div>
</aside>

<style>
  .callout {
    border: 0.5px solid;
    border-left-width: 2.5px;
    border-radius: 0 8px 8px 0;
    padding: 0.875rem 1.1rem;
    margin: 1.25rem 0;
  }
  .callout--warning {
    background: var(--docs-warn-bg);
    border-color: var(--docs-warn-border);
    border-left-color: var(--docs-warn-accent);
  }
  .callout__label {
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 0.8px;
    text-transform: uppercase;
    margin-bottom: 5px;
    display: flex;
    align-items: center;
    gap: 5px;
    color: var(--docs-warn-accent);
  }
  .callout--warning .callout__body { font-size: 13px; color: var(--docs-warn-text); line-height: 1.65; }
  .callout--warning .callout__body :global(code) {
    background: var(--docs-warn-code-bg);
    color: var(--docs-warn-code-fg);
    padding: 1px 5px;
    border-radius: 4px;
    font-family: var(--sl-font-mono);
    font-size: 11.5px;
  }
</style>
